import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dbueno.systemcont',
  appName: 'Imaginario-Playground',
  webDir: 'dist'
};

export default config;
